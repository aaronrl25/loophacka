import { zeroDemoWorkflows } from '../../data/zeroDemoWorkflows'
export function ToolResultCard(){const data=zeroDemoWorkflows.payroll;return <div className="tool-result"><header><span>✓</span><div><small>VALIDATED FINANCIAL RESULT</small><b>Three payroll alternatives found</b></div></header><div className="payroll-options">{data.alternatives.map((option,index)=><span className={index===0?'recommended':''} key={option.provider}><i>{index===0?'BEST FIT':`OPTION ${index+1}`}</i><b>{option.provider}</b><strong>${option.monthlyCost}/mo</strong><small>{Math.round(option.confidence*100)}% confidence</small></span>)}</div><footer><span>Current cost <b>${data.currentCost}/mo</b></span><span>Estimated savings <b>${data.recommendation.monthlySavings}/mo · ${data.recommendation.annualSavings}/yr</b></span></footer><p>No subscription was created. Review provider details and migration fees before taking action.</p></div>}
